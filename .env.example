PORT=3000
HOST=0.0.0.0
NODE_ENV=production
JWT_SECRET=remplacez-par-une-cle-secrete-longue-et-aleatoire

# Base de données PostgreSQL (OBLIGATOIRE en production)
# Render : créé automatiquement via render.yaml
# Local : installez Postgres ou utilisez Neon (gratuit) → https://neon.tech
DATABASE_URL=postgresql://user:password@localhost:5432/mindmap

# Fallback dev local sans Postgres (NON persistant sur Render)
DATA_DIR=./server/data

DEEPSEEK_API_KEY=
OPENAI_API_KEY=
